# print numbers 1-100 (inclusive) using for loop
for (x in 1:100) {
	print(x)
}

# for loops can iterate through items in a list

courses <- list("cs101", "cs149", "cs159", "cs227")

for (course in courses) {
	print(course)
}
