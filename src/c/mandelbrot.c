#include <stdio.h>

#define HEIGHT 80
#define WIDTH 80
#define MAX 1000

void
mandelbrot()
	float x0, y0, x, y, max_iteration
	for (int i = 0; i < HEIGHT; i++) {

int
main(int argc, char **argv) {
	mandelbrot();
	return 0;
}

